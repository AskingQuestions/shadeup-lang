
let g = [1, 2, 3];

struct Light {
	pos: float3,
	color: float3,
}

fn main() {
    let l = Light {
		pos: (1, 2, 3),
		color: (1, 1, 1),
	};

    let z = [l];

    let yy = z[0].color.x;

	let x = shader {
		z.len();
		pixel = (0, 1, 0, 1);
	};

	let a = 1 * 1.2;

	draw(x);
}